Ты — старший Python/Django архитектор.
Твоя задача — полностью сгенерировать готовую файловую структуру Django-проекта в соответствии с заданием ниже.
Выводи код строго в виде отдельных файлов, НЕ смешивай несколько файлов в одном блоке.
Структурируй всё как готовый проект, который можно запустить сразу после копирования.

================================
📌 ТЕХНИЧЕСКОЕ ЗАДАНИЕ
================================

Сделать монолитное Django-приложение (но с модульной структурой, чтобы его можно было разделить на микросервисы в будущем), реализующее систему регистрации и контроля дефектов на строительных объектах.

Используй:

Python 3.11+

Django 5+

Django REST Framework

PostgreSQL

django-filter

drf-spectacular (OpenAPI)

JWT (через SimpleJWT)

Pillow (вложения)

pytest (или unittest)

docker-compose (PostgreSQL + Django)

Makefile (опционально)

Проект должен быть максимально реалистичным и соответствовать промышленным стандартам.

================================
📌 1. РОЛИ И ПРАВА ДОСТУПА
================================

Создать три роли:
Менеджер, Инженер, Наблюдатель.

Описать permissions через DRF permissions + Django groups.

Роль: Менеджер

Права:

полный CRUD по проектам и этапам

создание дефектов

назначение исполнителей

изменение любого статуса

редактирование всех дефектов

просмотр всех отчётов

экспорт данных

просмотр всех пользователей

добавление комментариев к любым дефектам

Не может:

менять роли пользователей

Роль: Инженер

Права:

создание дефектов

редактирование только своих дефектов

загрузка вложений

изменение статуса: “В работе” → “На проверке”

добавление комментариев

просмотр только тех проектов, к которым он назначен

просмотр дефектов

Не может:

удалять дефекты

закрывать дефекты

назначать исполнителей

работать с отчётами

Роль: Наблюдатель

Права:

только просмотр проектов и дефектов

просмотр вложений

просмотр отчётов

просмотр истории изменений

добавление комментариев (опционально — включить)

Не может: ничего изменять.

================================
📌 2. СТРУКТУРА ПРОЕКТА
================================

Сформируй структуру файлов вот так:

backend/
  manage.py
  config/
    __init__.py
    settings.py
    urls.py
    wsgi.py

  users/
    models.py
    serializers.py
    views.py
    permissions.py
    urls.py
    tests.py

  projects/
    models.py
    serializers.py
    views.py
    urls.py
    tests.py

  defects/
    models.py
    serializers.py
    views.py
    permissions.py
    urls.py
    tests.py
    services.py   # логика переходов статусов

  reports/
    views.py
    serializers.py
    analytics.py
    urls.py
    tests.py

  media/   # вложения

Dockerfile  
docker-compose.yml  
requirements.txt  
Makefile (опционально)

================================
📌 3. МОДЕЛИ
================================
Users

username

email

role (manager/engineer/observer)

связка с Django permissions

Projects

title

description

start_date

end_date

status (active/closed)

stages (отдельная модель, FK)

Defects

project (FK)

stage (FK)

title

description

priority (low/medium/high)

status (new / in_progress / review / closed / cancelled)

performer (FK -> engineer)

deadline

created_at / updated_at

attachments (модель с FK + filefield)

history записей статусов

comments

Историю статусов реализовать отдельной моделью.

================================
📌 4. API
================================

Сделать следующие эндпоинты:

Auth (JWT)

/api/auth/login/

/api/auth/refresh/

Users

/api/users/

/api/users/{id}/

Projects

/api/projects/

/api/projects/{id}/

/api/projects/{id}/stages/

Defects

/api/defects/

/api/defects/{id}/

/api/defects/{id}/change_status/

/api/defects/{id}/attachments/

/api/defects/{id}/comments/

Reports

/api/reports/summary/

/api/reports/by_project/

/api/reports/by_engineer/

/api/reports/export/

API должно быть защищено ролями и permissions.

================================
📌 5. БИЗНЕС-ЛОГИКА
================================
Реализовать статусную машину:
new → in_progress → review → closed
new → cancelled
in_progress → cancelled


Переходы должны быть проверены в сервисном слое (services.py).

Добавить проверки:

инженер может редактировать только свои дефекты

менеджер может изменять всё

наблюдатель только читает

нельзя назначить наблюдателя исполнителем

нельзя закрыть дефект, если статус ≠ review

================================
📌 6. ФИЛЬТРАЦИЯ
================================

Использовать django-filter.

Фильтры для дефектов:

проект

исполнитель

статус

приоритет

временной диапазон

текстовый поиск

================================
📌 7. ТЕСТЫ
================================

Сделать:

минимум 5 unit-тестов

2 интеграционных теста:

создание дефекта → назначение → изменение статуса

фильтрация дефектов

тесты ролей:

менеджер может всё

инженер ограничен

наблюдатель read-only

================================
📌 8. DOCKER
================================

Сгенерировать:

Dockerfile

Python 3.11

gunicorn

collectstatic

docker-compose.yml

django backend

postgresql

volumes для media/

================================
📌 9. ВЫВОД РЕЗУЛЬТАТА
================================

Выводи результат строго в таком порядке:

Корневая структура файлов

requirements.txt

Dockerfile

docker-compose.yml

Полный код всех Django приложений

Полный код settings.py и urls.py

Примеры API-запросов (cURL)

Минимальные фикстуры data.json

Инструкции по запуску

Каждый файл — в отдельном блоке:

### backend/users/models.py
<код>